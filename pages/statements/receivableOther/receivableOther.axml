<import-sjs name="filter" from="../../../utils/filter.sjs"/>
<view class="page-content">
  <view class="pay-header">
    <view class="van-row">
      <view class="van-col van-col-16 left">
        <view class="title">其他应收</view>
        <view class="price">{{filter.numberToFixed(sumData.amt_end)}}</view>
      </view>
      <view class="van-col van-col-8 right ">
        {{sumData.account_month}}
      </view>
    </view>
  </view>
  <view class="pay-content">
    <view class="pay-title">其他应收明细</view>
    <view class="pay-detail">
      <block a:if="{{arData.length>0}}">
        <list-item class="font-12" a:for="{{arData}}" a:key="index" a:if="{{item.amt_end_cr != null}}">
          <text class="font-12">{{item.customer_name}}:</text>
          <view slot="extra">{{filter.numberToFixed(item.total)}}</view>
        </list-item>
      </block>
    </view>
  </view>
</view>